{
  "private": true,
  "version": "0.0.12",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "commander": "6.1.0",
    "semver": "7.3.2",
    "typescript": "4.0.2"
  },
  "scripts": {
    "build": "yarn workspaces run build"
  }
}